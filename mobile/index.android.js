/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './src/App';
import { name as EphemeralMobileMailClient } from './app.json';

AppRegistry.registerComponent(EphemeralMobileMailClient, () => App);
